## Folder Structure

Put all the "*.csv" files under `./data`, which will be ignored when you push your commits.

All analysis notebooks and code under `./analysis`, including `analysis.ipynb` the main analysis notebook and supplementary analysis for Figure 4 and Figure A2.

To set up your environment: `pip install -r requirements.txt`