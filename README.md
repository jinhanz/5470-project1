## Folder Structure

Put all the "*.csv" files under `./data`, which will be ignored when you push your commits.

All analysis notebooks and code under `./analysis`.

All figures / plots generated to be included in the written report under `figures`.

To set up your environment: `pip install -r requirements.txt`